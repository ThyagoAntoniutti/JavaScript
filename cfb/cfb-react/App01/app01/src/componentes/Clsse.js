import React from "react";

// No caso do componente de classe usamos a herança
class Classe extends React.Component {
    render() {
        return (
                
        )
    }
}

export default Classe